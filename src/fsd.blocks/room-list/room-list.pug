mixin roomList(label)
    .room-list&attributes(attributes)
        .room-list__label.title.title_h3 Удобства номера
        +dropdownOutput('Комнаты')
            .room-list__content
                +quantitativeList({bedrooms: 'спальни', beds: 'кровати', bathrooms: 'ванные комнаты'})