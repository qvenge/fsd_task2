'use strict';
require('./style.scss');

window.BEM.initAllEntities();

// const html = require('./some.pug');

// const uiForms = document.querySelectorAll('.ui-form-element');
// uiForms[uiForms.length - 1].innerHTML = html;

// const content = document.createElement('DIV');
// content.classList.add('ui-form-element__content');
// content.innerHTML = html;
// uiForms[uiForms.length - 2].append(content);
